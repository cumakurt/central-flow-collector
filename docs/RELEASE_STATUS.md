# Release Status — v4.0.0

v4.0.0 is the focused Central Flow Collector & Flow Analytics release. A final package is considered releasable only after cache-free Go tests, vet/static syntax checks, split race coverage, decoder fuzzing, query/decoder benchmarks, amd64/arm64 cross-builds, SBOM/checksum generation, UDP runtime smoke/load testing, package integrity and extracted-binary verification.

See `VALIDATION_REPORT_4.0.0.md` and `validation/` for the exact final run.
