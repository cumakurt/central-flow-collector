# Administration

The System/Admin APIs cover configuration validation/apply/rollback, users, API tokens, audit verification, backups, retention, storage checks, ClickHouse maintenance, enrichment reload and notification tests. Administrative operations are audited and return explicit failure on backend error.

NDR-specific maintenance actions are absent in v4. Operational alert rules are limited to flow-derived availability/volume/baseline conditions.
